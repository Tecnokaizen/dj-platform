# Logbook — 2026-09-13

## Platform Core Recovery (production)

### Audit

- Coolify MCP + SSH (`coolify-vps`) used for read-only inventory.
- Two Postgres resources found: **supabase-db** (full Supabase + Prisma init) and **platform-core-bd** (empty `dj_platform`).
- Declared **supabase-db** canonical; **platform-core-bd** legacy.
- Production baseline: Auth 2 users / 2 profiles; Prisma only `20260806172928_init`; Foundation tables absent.

### Backups

- Logical dumps of both DBs; PREMIGRATION dump
  `platform-core-supabase-prod-PREMIGRATION-20260913-1645.dump` verified
  (`pg_restore --list` + SHA256).
- Off-box copy on local Mac under `~/Backups/platform-core/2026-09-13/`
  (checksum MATCH). Not committed to Git.

### Dry-run

- Isolated restore → ownership runbook → full migrate pipeline.
- Clean dry-run: Prisma 8/0 rolled_back, Supabase 6, seed 5/12/27, validator PASS.
- Root cause for migrate failure: `public` objects owned by `postgres` while DDL uses `platform_migration`.

### Production window

- Bootstrap as `supabase_admin` (superuser); `REVOKE CREATE ON SCHEMA public FROM PUBLIC`.
- Ownership prep (public only; auth.* untouched).
- `db:migrate:release`: SUCCESS — Foundation recovered; stack healthy; legacy DB untouched.

### Tooling committed

- `prepare-public-schema-ownership.*`, bootstrap wiring, dry-run fidelity SQL (DRY-RUN ONLY), recovery docs.

### Next

- Deprecate `platform-core-bd` per phased plan (document → reference scan → stop later → observe → delete later).
- Afterwards: DJ Studio Domain extraction — not Product build yet.
