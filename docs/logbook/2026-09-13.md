# Logbook — 2026-09-13

## Platform Core Recovery (production)

### Audit

- Coolify MCP + SSH (`coolify-vps`) used for read-only inventory.
- Two Postgres resources found: **supabase-db** (full Supabase + Prisma init) and **platform-core-bd** (empty `dj_platform`).
- Declared **supabase-db** canonical; **platform-core-bd** legacy.
- Production baseline: Auth 2 users / 2 profiles; Prisma only `20260806172928_init`; Foundation tables absent.

### Backups

- Logical dumps of both DBs; PREMIGRATION dump
  `platform-core-supabase-prod-PREMIGRATION-20260913-1645.dump` verified
  (`pg_restore --list` + SHA256).
- Off-box copy on local Mac under `~/Backups/platform-core/2026-09-13/`
  (checksum MATCH). Not committed to Git.

### Dry-run

- Isolated restore → ownership runbook → full migrate pipeline.
- Clean dry-run: Prisma 8/0 rolled_back, Supabase 6, seed 5/12/27, validator PASS.
- Root cause for migrate failure: `public` objects owned by `postgres` while DDL uses `platform_migration`.

### Production window

- Bootstrap as `supabase_admin` (superuser); `REVOKE CREATE ON SCHEMA public FROM PUBLIC`.
- Ownership prep (public only; auth.* untouched).
- `db:migrate:release`: SUCCESS — Foundation recovered; stack healthy; legacy DB untouched.

### Tooling committed

- `prepare-public-schema-ownership.*`, bootstrap wiring, dry-run fidelity SQL (DRY-RUN ONLY), recovery docs.

### Legacy DB deprecation — Phase C STOP

- **STOP UTC:** 2026-09-13T18:36:33Z
- **Resource:** `platform-core-bd` / UUID `gww3fbml08nacbdqctsjh9nn`
- **Motivo:** Phase B = SAFE TO STOP; empty app schema; no active consumers
- **Método:** Coolify `StopDatabase` con `removeContainer=false`, `dockerCleanup=false` (sin `docker rm`, sin prune)
- **Post-STOP:** container `exited`; Coolify status `exited:unhealthy` (esperado al estar parado); `deleted_at=null`
- **Conservado:** resource Coolify, volume `postgres-data-gww3fbml08nacbdqctsjh9nn`, dumps PREMIGRATION
- **Canónica post-STOP:** supabase-db healthy; profiles=2; auth.users=2; Prisma finished=8; Supabase migrations=6; Kong/Auth healthy; PostgREST running
- **Observación:** no DELETE antes de **2026-09-20**; revisión recomendada **2026-09-27** (7–14 días)

### DJ Studio — Phase 1 Domain scaffold

- Created `src/domains/dj-studio/` with ownership README, contracts for
  profile/library/playlists, and P0 Domain permission keys (outside Core seed).
- No Prisma, migrations, RLS, UI, or Core→Domain imports.
- Validation: `npm run typecheck`, `npm run lint`, Domain permission-keys test PASS.

### DJ Studio — Phase 2 / M1 (local only)

- Migration `prisma/migrations/20260913220000_dj_studio_profile_and_legacy_namespace`
  — rename legacy DJ tables + create `dj_studio_profiles` + Profile field backfill.
- Validated on isolated local Postgres `dj_platform_m1` (Docker test compose).
- **Production not modified.**

### DJ Studio — Phase 2 / M2 (local only)

- Migration `prisma/migrations/20260913223000_dj_studio_library_and_tags`
  — CREATE `library_items`, Organization-scoped `tags`, `library_item_tags`.
- No backfill; new tables empty; legacy + `dj_studio_profiles` preserved.
- Constraint tests A–E PASS on `dj_platform_m1`; `migrate diff` = 0.
- Composite tenant FKs on join **deferred** (documented in schema + migration).
- **Production not modified.**

### DJ Studio — Phase 2 / M3 (local only)

- Migration `prisma/migrations/20260913224500_dj_studio_playlists`
  — CREATE org-scoped `playlists` + `playlist_items` (→ LibraryItem).
- Repeated LibraryItem allowed; UNIQUE `(playlistId, position)` only.
- Constraint tests A–G PASS; cross-org gap confirmed (EXPECTED GAP → M4).
- **Production not modified.**

### DJ Studio — Phase 2 / M4 (local only)

- Migration `prisma/migrations/20260913230000_dj_studio_tenant_backfill_and_integrity`
  — Organization bootstrap (oldest ACTIVE membership else personal org + OWNER),
  legacy → Domain backfill, composite tenant FKs.
- Mapping: `personal-<profile uuid without dashes>`; no PII in slug.
- Rich fixture + collision ABORT tests + isolation FK tests PASS locally.
- Legacy tables unchanged in counts; OWNER invariant held.
- **Production not modified.**

### DJ Studio — Phase 2 / M5 (local only)

- Supabase `20260913240000_dj_studio_rls_and_runtime_grants` — Domain RLS +
  legacy browser revoke + `app_runtime` Domain grants.
- Product seed composes Core foundation + `seedDjStudioDomainPermissions`
  (Core does not import Domain; `hasPermission` uses DB RolePermission).
- RLS tests A–H + app_runtime DML PASS; Domain registration idempotent.
- **Production not modified.**

### DJ Studio — Phase 2 / M6 (local only)

- Domain runtime: `resolveActiveOrganization`, `ensurePersonalOrganization`
  (slug `personal-<uuid>`, concurrency-safe), Library/Tag/Playlist/DjStudioProfile
  services with Domain RBAC.
- Cookie `active_organization_id` = preference only (validated membership).
- Server write path: auth → active org → permission → Domain service → Prisma.
- **Production not modified.**

### DJ Studio — Phase 2 / M7 (local only)

- Product routes: `/library`, `/playlists`, `/playlists/[id]`, `/studio-profile`.
- Private layout: active Organization resolution + switcher (>1 orgs), nav links.
- Server actions → M6 Domain services; VIEWER manage UI hidden (server enforced).
- Catalog search (`searchCatalogTracks`) for Add Track; Studio Profile SoT;
  Core profile form no longer writes `djName`; dashboard no longer shows `isAdmin`.
- **Production not modified.**

### DJ Studio — Full staging / dry-run validation (2026-09-14)

Initial verdict was **BLOCKED** (S1↔M1 order, M4/OWNER vs seed, validator totals).

### DJ Studio — Release pipeline fix (2026-09-14, local only)

**Verdict: FIX PASS** (ready to treat as staging-workflow validated locally).

Pipeline after fix:

1. bootstrap DB roles
2. Foundation Prisma (`prisma/migrations`)
3. Foundation Supabase S1–S7 (`supabase/migrations` via ledger-aware applier)
4. Foundation seed (`prisma/seed-foundation.ts`)
5. Domain Prisma M1–M4 (`prisma/migrations-dj-studio` + `prisma.dj-studio.config.ts`)
6. Domain Supabase M5 (`supabase/migrations-dj-studio`)
7. Product seed (`prisma/seed.ts`)
8. Key-based validator (`--foundation` / `--product`)

Evidence (disposable DBs only):

- Clean install `migrate-release` PASS (+ idempotent re-run)
- Production-like Foundation → Domain with legacy + 0 orgs → personal OWNER PASS
- Rich legacy from-scratch upgrade PASS
- M4 without OWNER aborts with no partial orgs; legacy preserved
- Domain tests 38 PASS; Core regressions PASS; build/typecheck/lint PASS

**Production not modified.** No deploy.

### DJ Studio — FULL FINAL VALIDATION (2026-09-14, local only)

**Verdict: READY FOR STAGING** (no commit/deploy in this pass).

Re-ran A/B/C + ledgers + validators + RLS/FK/RBAC/runtime + Domain suite +
typecheck/lint/build exclusively via canonical `npm run db:migrate:release`
(and Foundation-only phase where required). Evidence on disposable Supabase
local DBs (`dj_studio_final_*`). Production NOT MODIFIED.

Known residual (non-blocking for staging commit prep): shared
`dj_platform_test` Postgres has Foundation RLS + Domain schema applied; two
Core memberships tests that assume a bare test DB fail environmentally.
Disposable-DB Domain/tenant/permission suites PASS (38 Domain; focused Core
RLS/permissions PASS).

### Next

- Prepare commit/review of DJ-STUDIO-001 set before any staging deployment.
- Observation window for legacy DB until 2026-09-27 before DELETE consideration.
